let status = 'ligado';
const ligarDesligar = () => (
    status === 'ligado' ? status = 'desligado' : status = 'ligado'
    )

console.log(`O motor está ${ligarDesligar()}`);
console.log(`O motor está ${ligarDesligar()}`);
console.log(`O motor está ${ligarDesligar()}`);